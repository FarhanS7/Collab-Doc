{
  "name": "collabeditor-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.19.0",
  "scripts": {
    "build": "turbo run build",
    "dev:servers": "turbo run dev",
    "dev": "docker-compose up -d postgres redis && pnpm --filter @collab/server run db:migrate && turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test:load": "k6 run load-tests/collab-editor.js"
  },
  "devDependencies": {
    "turbo": "^2.0.4",
    "typescript": "^5.4.5"
  }
}
