{
  "name": "collabeditor-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.19.0",
  "scripts": {
    "build": "turbo run build",
    "dev:servers": "turbo run dev",
    "dev": "docker-compose up -d postgres redis && pnpm --filter @collab/server exec prisma migrate dev && turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.0.4",
    "typescript": "^5.4.5"
  }
}
